# this is a config test file



server.name = giffo web super server


site.title = test
site.motto = "do stuff"

timeout = what

redis.host = 2121
redis.port = lcoal
redis.password = yua80iip


# redis or anything else defaults to memory store
session.store = 21
# minutes
session.expire = 20
session.secret = j9j9j
#//session.secret = freedom
# md5 or sha or sha256 or sha512 - taken from utils.
session.hash = md5


#test

test.number = 12210
test.array = this, is , an, array
test.boolean = false




password.saltlen = 24
password.keylen = 40
password.iterations = 4000
